server:
  port: 8080
bot:
  telegram:
    botToken: ${secret.bot.telegram.token}
    botUsername: get_my_stocks_bot
    port: 8080
spring:
  application:
    name: telegram_service
  kafka:
    bootstrap-servers: PLAINTEXT://kafka:9092

  redis:
    host: redis
    port: 6379
    password: pass


topic:
  share: share.topic
  bond: bond.topic
  portfolio: portfolio.topic
  request: req.topic
  share_res: share_res.topic
  bond_res: bond_res.topic
  portfolio_res: portfolio_res.topic

